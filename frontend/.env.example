# 로컬 개발 (PC에서만)
VITE_API_BASE=http://127.0.0.1:8000/api

# Vercel 배포 (pv-five-wine.vercel.app)
# vercel.json 이 /api/* → https://pv-qce5.onrender.com/api/* 로 프록시합니다.
# 별도 VITE_API_BASE 설정 불필요 (127.0.0.1 은 모바일에서 동작하지 않음).
